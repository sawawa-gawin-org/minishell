name: run Norminette

on:
  pull_request:
    branches: [ "main" ]

jobs:
  run_norminette:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-python@v4
        with:
          python-version: '3.12'
      - name: Install norminette
        run: |
          python -m pip install --upgrade pip setuptools
          pip install norminette
      - run: norminette -d || true